#!/usr/bin/env oh

define common: import: "/"::join $origin common.oh

dynamic $stdout: open "w": "/"::join $origin .. boot.go

common::introduction @`(basename $0) $GOPACKAGE

echo "var boot string = `"
cat: "/"::join $origin .. scripts boot.oh
echo "`"

