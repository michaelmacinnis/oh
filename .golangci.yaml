linters:
  enable-all: true
  disable:
    - exhaustivestruct
    - scopelint
    - wrapcheck

linters-settings:
  exhaustive:
    default-signifies-exhaustive: true
  funlen:
    lines: 96

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
