#!/usr/bin/env oh

define $dir: "/"::join $origin .. doctest

# Run tests.
find $dir -name "[0-9]*.oh" | grep -Fv unused |
while (define $file: readline) {
	grep "^#[+-]     " $file | sed -e "s/^#[+-]     //g"
	$file
} | sort | uniq -u

